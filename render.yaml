# Render Blueprint Specification (Infrastructure as Code)
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: astittva-backend
    runtime: python
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker server:app --bind 0.0.0.0:$PORT --workers 2
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DB_NAME
        value: astitva_realestate
      - key: MONGO_URL
        sync: false # Set your MongoDB Atlas URI: mongodb+srv://<user>:<password>@cluster0.xxxx.mongodb.net/?retryWrites=true&w=majority
      - key: JWT_SECRET
        generateValue: true # Render will automatically generate a secure 256-bit secret key
      - key: ADMIN_EMAIL
        value: admin@astitva.com
      - key: ADMIN_PASSWORD
        sync: false # Set your admin password, e.g., Astitva@2026
      - key: ADMIN_NAME
        value: Astitva Admin
      - key: CORS_ORIGINS
        value: "https://astittva.in,https://www.astittva.in,http://localhost:3000"
      - key: FRONTEND_URL
        value: https://astittva.in
      - key: APP_NAME
        value: astitva-realestate
      - key: RESET_ADMIN_PASSWORD_ON_START
        value: "false"
      - key: EMERGENT_LLM_KEY
        sync: false # Optional: legacy Emergent object storage key

